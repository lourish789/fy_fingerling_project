# Placeholder for trained model weights
# 
# After training your model, place the weights file here:
# - best.pt (best performing weights)
# - last.pt (last checkpoint)
#
# Training command:
# python -m src.training.train --data data/fingerlings.yaml --epochs 100
